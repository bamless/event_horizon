import event_horizon.errors as _
import event_horizon.uv.event_loop for EventLoop

// private - the global event loop
static var loop = EventLoop()

/// Return the process-global EventLoop used by default handles.
fun loop()
    return loop
end
