import event_horizon.data_structures.registry for Registry

native getAddrInfo(callback, node=null, service=null, flags=0, family=0, sockType=0, protocol=0,
                   loop=null)

// -----------------------------------------------------------------------------
// Private implementation
// -----------------------------------------------------------------------------

static var callbacks = Registry()

fun _callbacks()
    return callbacks
end
