import event_horizon as evh
import event_horizon.handle for Handle, HandleType

class Idle is Handle
    construct(loop=null)
        super(loop or evh.loop(), HandleType.IDLE)
    end

    native start(callback)
    native stop()
end
